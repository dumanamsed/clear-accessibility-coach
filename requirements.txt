flask>=3.0
python-pptx>=0.6.23
python-docx>=1.1.2
PyMuPDF>=1.24
beautifulsoup4>=4.12
Pillow>=10.0
anthropic>=0.40
python-dotenv>=1.0
gunicorn>=21.0
# Server-side PDF export. Needs native libs (pango/cairo) — present on Linux
# hosts like Render; on machines without them the app falls back to the
# browser print dialog automatically.
weasyprint>=62.0
